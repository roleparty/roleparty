package app.roleparty.application

import io.kotest.core.spec.style.ShouldSpec

class CreateCampaignServiceTest : ShouldSpec({

    context("when user creates a campaign") {
        context("but error occurs during creation") {
            should("throw an exception") {

            }
        }
        should("create a campaign") {

        }
    }
})